const subject = `Subscription refused`;

const html = `<h1>Subscription refused</h1><p>Your subscription has been refused.<p>`;

const text = `Subscription refused\n\nYour subscription has been refused.`;

module.exports = {
  subject,
  text,
  html,
};
