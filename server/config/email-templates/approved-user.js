const subject = `Subscription approved`;

const html = `<h1>Subscription approved</h1><p>Your subscription has been approved.<p>`;

const text = `Subscription approved\n\nYour subscription has been approved.`;

module.exports = {
  subject,
  text,
  html,
};
